{
  "name": "@jk/edge-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "On-farm edge gateway (§34): ingests RFID/scale device readings over the LAN, buffers them durably on local disk, and batches them upstream to the platform API with the offline-sync engine's never-lose guarantees. Runs where connectivity is intermittent.",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "test:unit": "vitest run --passWithNoTests",
    "test:integration": "vitest run tests/integration --passWithNoTests"
  },
  "dependencies": {
    "@jk/contracts-rest": "workspace:*",
    "@jk/domain-kernel": "workspace:*",
    "@jk/offline-sync": "workspace:*",
    "@jk/sync-http": "workspace:*",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.9"
  }
}
