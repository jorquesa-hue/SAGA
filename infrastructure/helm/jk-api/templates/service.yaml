apiVersion: v1
kind: Service
metadata:
  name: {{ include "jk-api.fullname" . }}
  labels:
    {{- include "jk-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "jk-api.selectorLabels" . | nindent 4 }}
