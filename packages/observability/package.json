{
  "name": "@jk/observability",
  "version": "0.1.0",
  "private": true,
  "description": "Structured logging (secret-safe), correlation IDs, and OpenTelemetry baseline. JK-PLT-EES-001 §77, JK-SEC-006.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "vitest run --passWithNoTests",
    "test:integration": "echo 'no integration tests for observability'"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^3.2.6"
  }
}
