{
  "name": "@jk/contracts-rest",
  "version": "0.1.0",
  "private": true,
  "description": "Typed REST client for the SAGA API (Volume VI). Mirrors the authored OpenAPI contract: tenant/auth headers, idempotency keys, correlation propagation, and RFC 9457 Problem Details error mapping. Consumed by the web and mobile apps.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "vitest run --passWithNoTests",
    "test:integration": "vitest run tests/integration --passWithNoTests"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^3.2.6"
  }
}
