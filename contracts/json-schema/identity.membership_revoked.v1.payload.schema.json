{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.jk.example/json-schema/identity.membership_revoked.v1.payload.schema.json",
  "title": "identity.membership_revoked.v1 payload",
  "description": "Payload of identity.membership_revoked.v1 (aggregate: user). Membership history is append-only: revocation is a new fact, never a deletion.",
  "type": "object",
  "required": ["userId", "role", "reason"],
  "additionalProperties": false,
  "properties": {
    "userId": {
      "type": "string",
      "format": "uuid"
    },
    "role": {
      "$ref": "#/$defs/role"
    },
    "reason": {
      "type": ["string", "null"],
      "maxLength": 500,
      "description": "Optional human-readable revocation reason; null when not given."
    }
  },
  "$defs": {
    "role": {
      "type": "string",
      "description": "Canonical tenant-membership roles (platform_admin is platform-level, never a membership role).",
      "enum": [
        "tenant_owner",
        "farm_manager",
        "technician",
        "veterinarian",
        "genetics_specialist",
        "finance_user",
        "auditor",
        "integration_service"
      ]
    }
  }
}
